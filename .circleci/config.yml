version: 2.1

orbs:
  queue: eddiewebb/queue@1.1.2

commands:
  checkout-specs:
    steps:
      - run:
          name: Checkout
          command: |
            mkdir -p ~/.ssh

            echo 'github.com ssh-rsa ' >> ~/.ssh/known_hosts

            if [ -e "/home/circleci/project/.git" ]
            then
              cd "/home/circleci/project"
              git remote set-url origin "$CIRCLE_REPOSITORY_URL" || true

              echo "Fetching $CIRCLE_BRANCH ..."
              git fetch --force origin "$CIRCLE_BRANCH:remotes/origin/$CIRCLE_BRANCH"

              echo "Resetting to origin/$CIRCLE_BRANCH"
              git reset --hard "origin/$CIRCLE_BRANCH"
            else
              mkdir -p "/home/circleci/project"
              cd "/home/circleci/project"
              echo "Cloning $CIRCLE_BRANCH ..."
              git clone "$CIRCLE_REPOSITORY_URL" --single-branch --branch "$CIRCLE_BRANCH" .
            fi

jobs:
  update-indexes:
    docker:
       - image: circleci/ruby:2.4.1
    environment:
      INDEXES_BRANCH: indexes-forced-no-rebase
    steps:
      - queue/until_front_of_line
      - restore_cache:
          keys:
            - forced-no-rebase-spec-indexes-source-v2-{{ .Branch }}-{{ .Revision }}
            - forced-no-rebase-spec-indexes-source-v2-{{ .Branch }}-
            - forced-no-rebase-spec-indexes-source-v2-
      - checkout-specs
      - run:
          name: Generate indexes for all pods
          command: |
            ./Scripts/add_indexes.sh
      - run:
          name: Git config
          command: |
            git config --global user.email "$CIRCLE_USERNAME@users.noreply.github.com"
            git config --global user.name "$CIRCLE_USERNAME"
      - add_ssh_keys:
          fingerprints:
            - "39:12:25:06:c2:71:00:c6:7c:fe:40:a6:e7:0e:18:ed"
      - run:
          name: Commit and force push the generated indexes
          command: |
            git add -A
            git commit --quiet -m "Add CDN indexes for all pods"
            git push origin $CIRCLE_BRANCH:$INDEXES_BRANCH --force
      - save_cache:
          key: forced-no-rebase-spec-indexes-source-v2-{{ .Branch }}-{{ .Revision }}
          paths:
            - ".git"

workflows:
  update-indexes:
    jobs:
      - update-indexes:
          filters:
            branches:
              only: master-forced-no-rebase
